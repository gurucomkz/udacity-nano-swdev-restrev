'use strict';

/**
 * @ngdoc function
 * @name restRevApp.controller:RestaurantlistCtrl
 * @description
 * # RestaurantlistCtrl
 * Controller of the restRevApp
 */
angular.module('restRevApp')
  .controller('RestaurantlistCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
